<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Journal Entry</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f549;
            color: #333;
            background-image: url('/css/mindfulness.svg'); /* Path to your SVG background image */
            background-size: 50% auto; /* Scale the image to cover the entire page */
            background-position: center; /* Center the image */
            background-repeat: no-repeat; /* Prevent repeating the image */
            background-attachment: fixed; /* Fix the background image */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: rgba(255, 255, 255, 0.421); /* White background with slight opacity */
            padding: 20px;
            border-radius: 8px;
            
            width: 100%;
            max-width: 600px;
            position: absolute; /* Use absolute positioning */
            left: 20px; /* Adjust this value to move it more to the left */
            top: 50%; /* Center vertically */
            transform: translateY(-50%); /* Adjust for vertical centering */
        }
        h2 {
            margin-bottom: 20px;
            color: #007bff;
        }
        textarea {
            width: 100%;
            height: 200px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
            box-sizing: border-box;
            resize: vertical;
        }
        button {
            background-color: #007bffd3;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #3479c3;
        }
        .back-button {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            color: #007bff;
            background: none;
            border: 1px solid #007bff;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s, color 0.3s;
        }
        .back-button:hover {
            background-color: #007bff;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Write Your Journal Entry</h2>
        <form action="/submit-journal" method="POST">
            <textarea name="journalEntry" placeholder="Write your thoughts here..."></textarea>
            <br>
            <button type="submit">Submit</button>
        </form>
        <a href="/" class="back-button">Back to Home</a>
    </div>
</body>
</html>
